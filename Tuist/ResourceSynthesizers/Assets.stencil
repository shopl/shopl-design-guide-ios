// swiftlint:disable all
// swift-format-ignore-file
// swiftformat:disable all
// Generated using tuist — https://github.com/tuist/tuist

{# Colors 또는 Images 카탈로그 존재 여부 확인 #}
{%- set isDesignSystem -%}{%- for catalog in catalogs -%}{%- if catalog.name == "Colors" or catalog.name == "Images" -%}1{%- endif -%}{%- endfor -%}{%- endset -%}

{% if isDesignSystem %}
import SwiftUI
import UIKit

// 번들 위치 찾기
private class DesignSystemBundleAnchor {}
private let dsBundle = Bundle(for: DesignSystemBundleAnchor.self)

// MARK: - 🖍️ Recursive Macros

{#- [MACRO] SDG.Color 정의 -#}
{%- macro recursiveColorLet assets -%}
  {% for asset in assets %}
    {% if asset.type == "color" %}
    static let {{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }} = SDG.Color(name: "{{ asset.value }}", bundle: dsBundle)
    {% elif asset.items %}
    {% call recursiveColorLet asset.items %}
    {% endif %}
  {% endfor %}
{%- endmacro -%}

{#- [MACRO] SwiftUI.Color static 변수 -#}
{%- macro recursiveColorStaticVarForSwiftUI assets -%}
  {% for asset in assets %}
    {% if asset.type == "color" %}
    static let {{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }} = SwiftUI.Color(SDG.Color.{{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }})
    {% elif asset.items %}
    {% call recursiveColorStaticVarForSwiftUI asset.items %}
    {% endif %}
  {% endfor %}
{%- endmacro -%}

{#- [MACRO] UIColor static 변수 -#}
{%- macro recursiveColorStaticVarForUIKit assets -%}
  {% for asset in assets %}
    {% if asset.type == "color" %}
    static let {{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }} = UIColor(sdg: SDG.Color.{{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }})
    {% elif asset.items %}
    {% call recursiveColorStaticVarForUIKit asset.items %}
    {% endif %}
  {% endfor %}
{%- endmacro -%}

{#- [MACRO] ShapeStyle static 변수 -#}
{%- macro recursiveShapeStyle assets -%}
  {% for asset in assets %}
    {% if asset.type == "color" %}
    static var {{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }}: SwiftUI.Color { SwiftUI.Color(SDG.Color.{{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }}) }
    {% elif asset.items %}
    {% call recursiveShapeStyle asset.items %}
    {% endif %}
  {% endfor %}
{%- endmacro -%}

{#- [MACRO] SDG.Image 정의 -#}
{%- macro recursiveImageLet assets -%}
  {% for asset in assets %}
    {% if asset.type == "image" %}
    static let {{ asset.name|swiftIdentifier:"pretty"|lowerFirstWord }} = SDG.Image(name: "{{ asset.value }}", bundle: dsBundle)
    {% elif asset.items %}
    {% call recursiveImageLet asset.items %}
    {% endif %}
  {% endfor %}
{%- endmacro -%}


// MARK: - 🧱 SDG Namespace

public enum SDG {
    
    // MARK: - Image Resource Struct
    public struct Image: Equatable {
        public let name: String
        public let bundle: Bundle
        
        public init(name: String, bundle: Bundle) {
            self.name = name
            self.bundle = bundle
        }
    }

    // MARK: - Color Resource Struct
    public struct Color: Equatable {
        public let name: String
        public let bundle: Bundle
        
        public init(name: String, bundle: Bundle) {
            self.name = name
            self.bundle = bundle
        }
    }
    
    public struct Font: Equatable {
        private init() { }
    }
}

// MARK: - Conversion Helpers

public extension SDG.Image {
    var image: SwiftUI.Image {
        SwiftUI.Image(sdg: self)
    }
    
    var uiImage: UIKit.UIImage {
        UIKit.UIImage(sdg: self)
    }
}

public extension SDG.Color {
    var color: SwiftUI.Color {
        SwiftUI.Color(self)
    }
    
    var uiColor: UIKit.UIColor {
        UIKit.UIColor(sdg: self)
    }
}


// MARK: - 🖼️ Image Extensions

public extension SDG.Image {
    {% for catalog in catalogs %}
    {% if catalog.name == "Images" %}
    {% call recursiveImageLet catalog.assets %}
    {% endif %}
    {% endfor %}
}

public extension SwiftUI.Image {
    /// 사용법: Image(sdg: .iconName)
    init(sdg resource: SDG.Image) {
        self.init(resource.name, bundle: resource.bundle)
    }
}

public extension UIImage {
    /// 사용법: UIImage(sdg: .iconName)
    convenience init(sdg resource: SDG.Image) {
        self.init(named: resource.name, in: resource.bundle, compatibleWith: nil)!
    }
}


// MARK: - 🎨 Color Extensions

public extension SDG.Color {
    {% for catalog in catalogs %}
    {% if catalog.name == "Colors" %}
    {% call recursiveColorLet catalog.assets %}
    {% endif %}
    {% endfor %}
}

public extension SwiftUI.Color {
    /// 내부 전용 생성자
    internal init(_ resource: SDG.Color) {
        self.init(resource.name, bundle: resource.bundle)
    }

    /// 사용법: Color.neutral100
    {% for catalog in catalogs %}
    {% if catalog.name == "Colors" %}
    {% call recursiveColorStaticVarForSwiftUI catalog.assets %}
    {% endif %}
    {% endfor %}
}

public extension UIColor {
    /// 내부 전용 생성자
    internal convenience init(sdg resource: SDG.Color) {
        self.init(named: resource.name, in: resource.bundle, compatibleWith: nil)!
    }

    /// 사용법: UIColor.neutral100
    {% for catalog in catalogs %}
    {% if catalog.name == "Colors" %}
    {% call recursiveColorStaticVarForUIKit catalog.assets %}
    {% endif %}
    {% endfor %}
}

// MARK: - ShapeStyle Extensions

public extension ShapeStyle where Self == SwiftUI.Color {
    /// 사용법: .background(.neutral100)
    {% for catalog in catalogs %}
    {% if catalog.name == "Colors" %}
    {% call recursiveShapeStyle catalog.assets %}
    {% endif %}
    {% endfor %}
}

{% else %}
// ⚠️ [Skip] 'Colors' 또는 'Images' 카탈로그가 발견되지 않아 코드를 생성하지 않았습니다.
{% endif %}
